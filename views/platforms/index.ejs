<%- include("../partials/header"); %>

<table id="list">
    <thead>
      <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Industry</th>
        <th>More Info</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>
       <% platforms.forEach(p => { %>
        <tr>
            <td><%= p.name %></td>
            <td><%= p.description %></td>
            <td><%= p.industry %></td>
            <td>
                <form action="/companies/<%= p._id %>" method="GET">
                    <input type="submit" value="More Info">
                </form>
            </td>
            <td>
                <form action="/platforms/<%= p._id %>" method="GET">
                    <input type="submit" value="Edit">
                </form>
            </td>
            <td>
                <form action="/platforms/<%= p._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
            </td>
        </tr>
       <% }); %>
    </tbody>
</table>

<%- include("../partials/footer"); %>